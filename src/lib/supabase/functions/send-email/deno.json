{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-read index.ts",
    "deploy": "cd ../../../../.. && supabase functions deploy send-email --no-verify-jwt"
  },
  "comments": {
    "deployment": "The deploy task assumes you're running it from the send-email directory; it navigates back to project root first"
  }
}
